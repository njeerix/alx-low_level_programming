#include "main.h"
/**
 * _abs - Computes the absolute value of an integer
 * @n: The number whose the absolute is computed.
 * Return: n if n is greater than or equal is 0.
 * -n if n isless than zero.
 */
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
return (n);
}
