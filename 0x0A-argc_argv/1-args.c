#include <stdio.h>
/**
 * main - entry point
 * @argc: The number of passed to the program
 * @argv: An array of strings containing the arguments
 * Return: Always 0 (Success)
 */
int main(int argc, char *argv[] __attribute__((unused)))
{
printf("%d\n", argc - 1);
return (0);
}
